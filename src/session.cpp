#include "session.hpp"

namespace gcrypt::session
{
    bytedata messaging_session::construct_byte_message(const bytespan& data)
    {
        bytedata out;
    }
    bytedata messaging_session::receive(const bytespan& data)
    {

    }
}